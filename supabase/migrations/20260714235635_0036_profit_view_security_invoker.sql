-- 0036: АУДИТ-БЛОКЕР A2 (внешний аудит 14.07): v_project_profit исполнялась правами владельца БД
-- и ОБХОДИЛА RLS — финансы проектов были видны любой авторизованной роли прямым запросом.
-- Переводим на security_invoker: цифры считаются правами запрашивающего.
-- Следствие по закону §1: у ролей без финдоступа стоимость труда/маржа опустеют (profile_rates финанс-гейтед) — это ПРАВИЛЬНО.

alter view public.v_project_profit set (security_invoker = true);